# Enable Corepack for package manager version management
enable-pre-post-scripts=true

# Pnpm configuration
auto-install-peers=true
shamefully-hoist=false
strict-peer-dependencies=false

# Lockfile settings
lockfile=true

# Node version (ensure compatibility)
node-version=20.0.0
