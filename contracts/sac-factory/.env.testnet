# SAC Factory Testnet Configuration

# Network
STELLAR_NETWORK=testnet
STELLAR_RPC_URL=https://soroban-testnet.stellar.org:443

# Identity (use 'dev' key generated with stellar CLI)
DEPLOYER_IDENTITY=dev

# Contract will be deployed and ID will be saved here
# SAC_FACTORY_CONTRACT_ID=<will_be_set_after_deployment>
