{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "corepack enable && corepack prepare pnpm@8.15.0 --activate && pnpm install --frozen-lockfile=false",
  "buildCommand": "pnpm --filter=frontend build",
  "framework": "nextjs"
}
